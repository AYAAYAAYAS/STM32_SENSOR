# KEIL问题相关
## stm32 在keil中跳转定义失败原因
+ **在Keil中，如果在代码中使用了函数指针，并且函数指针的定义在其他文件中，则在Keil中编译时会出现跳转定义失败的错误。**
___
解决方法：

 1. 将函数指针的定义和实现放在同一个文件中。
 2. 使用宏定义来代替函数指针。       
___
+ **路径选择了中文路径**
___
解决方法：
 1. 选择英文路径。
 2. 或者在Keil中设置路径为英文路径。
